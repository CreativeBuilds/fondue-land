{
  "name": "@scaffold-eth/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ fondueland/tickets",
    "create-local": "graph create --node http://localhost:8020/ fondueland/tickets",
    "remove-local": "graph remove --node http://localhost:8020/ fondueland/tickets",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fondueland/tickets"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.1",
    "@graphprotocol/graph-ts": "^0.20.1"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
