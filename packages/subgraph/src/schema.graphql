type Purchase @entity {
  id: ID!
  createdAt: BigInt!
  amount: BigInt!
  isPresale: Boolean!
  purchaser: Bytes!
}

type Deposit @entity {
  id: ID!
  createdAt: BigInt!
  amount: BigInt!
  depositer: Bytes!
}