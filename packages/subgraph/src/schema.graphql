type Purchase @entity {
  id: ID!
  createdAt: BigInt!
  amount: BigInt!
  cost: BigInt!
  isPresale: Boolean!
  purchaser: Bytes!
}

type TakeThePot @entity {
  id: ID!
  createdAt: BigInt!
  amount: BigInt!
  depositer: Bytes!
}